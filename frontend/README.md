
  # Axora Frontend

  This frontend started as a Figma-exported React/Vite bundle and is now the working `frontend/` app for Axora.

  ## Running the code

  Run `npm install` to install dependencies.

  Run `npm run dev` to start the development server.
  
